[workspace]
resolver = "3"
members = ["apps/counter", "apps/recorder"]
[workspace.package]
edition = "2024"

[profile.release]
opt-level = 2 # fast and small wasm

# Optimize all dependencies even in debug builds:
[profile.dev.package."*"]
opt-level = 2
