import { css } from "@emotion/react";

export const styles = {
	root: css({
		display: "flex",
		justifyContent: "center",
	}),
	stack: css({
		maxWidth: "210mm",
		padding: "0 10mm 10mm 10mm",
		width: "100%",
	}),
};
