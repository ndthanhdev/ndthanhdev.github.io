# yaml-language-server: $schema: 'https://moonrepo.dev/schemas/project.json'
tags:
  - trunk
toolchains:
  default: rust
tasks:
  build:
    deps:
      - ~:trunk-build
project:
  description: app
  title: "@n8v/counter"
layer: application
